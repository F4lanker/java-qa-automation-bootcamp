1. Ответ Б - Композиция. Логически наш АПИ тест не является notifier-ом (концепция IS-A), но у него может быть notifier (HAS-A). Нам не надо наследовать и переопределять все методы от нотифаера, используем только то что нам надо.
2. Используем наследование. Здесь PostPutDeleteTest явлется АПИ тестом(концепция IS-A). У него обязательно будет requestSpec, оформленный по шаблону родительского класса. Про дублирование ничего сказать не могу, нужен комментарий чтобы разобраться.
3. Композиция единственный вариант, потому что в Java нет множественного наследования.  BaseApiTest будет родиетельским классом (концепция IS-A), а BaseDatabaseTest (HAS-A)